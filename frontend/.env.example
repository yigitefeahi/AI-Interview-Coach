# Copy to .env.local for local development.
# NEXT_PUBLIC_* is inlined at `next build` — set the same values in CI/production.

NEXT_PUBLIC_API_BASE=http://localhost:8000

# Production (Render):
# NEXT_PUBLIC_API_BASE=https://ai-coach-backend-4ph0.onrender.com

# Optional: Playwright local port override
# PLAYWRIGHT_DEV_PORT=3000
